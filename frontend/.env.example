# FastAPI origin. Leave empty for the local Vite /api proxy. For a separately
# hosted backend, set the HTTPS origin without /api/v1.
VITE_API_BASE_URL=

# Never add a production API key to a VITE_* variable. Vite embeds those values
# in the public browser bundle. Production credentials are entered at runtime.
